<snippet>
	<content><![CDATA[
template <class T> T search2(T l, T r)
{
	while (l < r) {
		T mid = l + (r - l + 1) / 2;
		(cal(mid)) ? l = mid : r = mid - 1;
	}
	return l;
}

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>search2</tabTrigger> -->
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.c++</scope> -->
</snippet>
